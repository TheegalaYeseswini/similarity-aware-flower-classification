# Models

This directory is reserved for lightweight model metadata, architecture summaries, and future exported inference artifacts.

Large binary checkpoint files are intentionally excluded from version control. Keep them local or publish them through a release artifact or model-hosting service.
